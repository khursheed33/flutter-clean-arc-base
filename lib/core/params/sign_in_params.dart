/// The `SignInParams` class represents the parameters required for signing in, including a username and
/// password.
class SignInParams {
  final String username;
  final String password;

  SignInParams({
    required this.username,
    required this.password,
  });
}
